pipeline {
    agent none
    stages {
        stage('back-end') {
            agent { docker { image 'ubuntu:latest' }
            steps {
               mkdir new 
               echo "hello world" > new/touc.txt    
           }
        }
    }
}